# Portfolio - Jessé Oliveira (Next.js scaffold)

This scaffold contains the structure to run locally and deploy on Vercel. To run locally:

1. Install dependencies:
   ```bash
   npm install
   ```
2. Run dev server:
   ```bash
   npm run dev
   ```

Files included:
- app/ (Next.js app router pages)
- components/ (Header, Footer, CaseCard, placeholders)
- public/assets/images/cases/ (your case images)

Replace placeholder texts and paste PowerBI embed codes in the Case pages when ready.

Contact: Jesse_ombd@hotmail.com
